#follow_link.users-follow-link.d-flex.justify-content-center
  a href="#{ user_followings_path(user)}" class="m-2"
    p = user.following.count
    'フォロー
  a href="#{ user_followers_path(user) }" class="m-2"
    p = user.followers.count
    'フォロワー
